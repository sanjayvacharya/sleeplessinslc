eureka.region=default

eureka.name=eureka

eureka.vipAddress=eureka

eureka.port=${eureka.server.port}

eureka.shouldUseDns=false

eureka.serviceUrl.default=http://localhost:${eureka.server.port}/eureka/v2/

eureka.lease.renewalInterval=5